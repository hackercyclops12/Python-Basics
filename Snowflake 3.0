from turtle import *
import random
bgcolor('dark blue')
speed('fastest')
color('cyan')
penup()
forward(90)
left(45)
pendown()
def branch():
    for i in range (3):
        for i in range (3):
            forward(30)
            backward(30)
            right(45)
        left(90)
        backward(30)
        left(45)
    right(90)
    forward(90)
for i in range (8):
    branch()
    left(45)
    
        