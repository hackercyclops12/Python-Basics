from turtle import *
penup()
pensize(30)
bk(250)
while True:
	pendown()
	circle(10)
	penup()
	forward(100)
	pendown()
	circle(10)
	penup()
	forward(100)
	pendown()
	circle(10)
	penup()
	forward(100)
	pendown()
	circle(10)
	penup()
	forward(100)
	pendown()
	circle(10)
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	undo()
	